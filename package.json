{
  "name": "midi",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "frontend:ci": "npm --prefix frontend ci --no-audit --no-fund",
    "frontend:build": "npm --prefix frontend run build",
    "build": "npm run frontend:ci && npm run frontend:build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "easymidi": "^3.1.0",
    "express": "^5.1.0",
    "wrtc": "^0.4.7",
    "ws": "^8.18.3"
  }
}
